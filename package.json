{
  "name": "flickr-gallery",
  "version": "1.0.0",
  "description": "Gallergy created using Flickr API",
  "main": "lightbox.js",
  "scripts": {
    "start": "browser-sync start --directory --server --files '*.css, *.html, *.js' --https"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
     "browser-sync": "^2.12.5", 
    "gulp": "^3.9.1"
  }
}
